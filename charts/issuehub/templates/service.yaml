apiVersion: v1
kind: Service
metadata:
  name: {{ include "issuehub.fullname" . }}
  labels:
    {{- include "issuehub.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "issuehub.selectorLabels" . | nindent 4 }}
